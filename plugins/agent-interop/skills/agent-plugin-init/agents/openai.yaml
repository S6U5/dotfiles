interface:
  display_name: "Agent Plugin Init"
  short_description: "Create a plugin that reaches Claude Code, Codex and the standard"
  default_prompt: "Use $agent-plugin-init to set up a plugin that works across Claude Code and Agent Plugins clients."
