# dotfiles

個人の dotfiles を管理するリポジトリ。

## 運用方針

- 現時点では設定ファイルを**残す(コミットする)だけ**でよい。インストールスクリプトや管理ツールの導入は後回し。
- **OSS として公開予定**(現在はプライベート)。以下は絶対にコミットしない:
  - APIキー、トークン、パスワードなどの機密情報
  - プライベートなパス(実際のユーザー名を含む絶対パスなど)
  - メールアドレスや個人情報を含む設定(公開しても問題ないものは除く)
- 機密になりうる値は環境変数や `*.local` ファイル(gitignore 対象)に分離する。
