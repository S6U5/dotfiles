# pnpm のサプライチェーン攻撃対策(グローバル設定)
# npm と共有の ~/.npmrc に書くと npm が「未知のキー」警告を出すため、pnpm 固有設定はここに置く。
# macOS でもこのパス(~/.config/pnpm/rc)が読まれるよう、env.sh で XDG_CONFIG_HOME を設定している。

# 公開から 7 日(10080 分。単位は分)未満のバージョンを導入しない。
# 侵害されたバージョンは公開直後に検出・削除されることが多いため、少し寝かせてから入れる。
minimum-release-age=10080
