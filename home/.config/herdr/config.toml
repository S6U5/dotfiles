# herdr のキーバインドを tmux のデフォルトに揃える(プレフィックスは Ctrl+b のまま)

[keys]
# プレフィックスキー + % でペインを垂直分割、+ " で水平分割(tmuxのデフォルト split-window -h/-v に合わせる。
# herdr 自体のデフォルトは prefix+v / prefix+minus のため明示的に上書きする)
split_vertical = "prefix+%"
split_horizontal = 'prefix+"'

# ワークスペース選択(navigate mode)を矢印キーだけでなく hjkl でも操作できるようにする
# (ペイン移動 h/j/k/l はデフォルトで既に有効。ワークスペース上下だけデフォルトが矢印キーのみだった)
navigate_workspace_up = "k"
navigate_workspace_down = "j"

[ui]
agent_panel_sort = "priority"

# サイドバーのスペース一覧はデフォルト(row_gap = 0)だと隣のスペースと行が密着して
# 境目が分かりにくいため、エントリ間に空行を1行入れる(要 herdr v0.7.4 以上)
[ui.sidebar.spaces]
row_gap = 1
# スペース名の行を色付き太字にして、2行目(ブランチ・git状態)との区別を付ける
# (行単位のスタイルはトークンごとの fg/bold/dim のみ。行の背景色は v0.7.5 では変更不可。
#  fg は #RGB / #RRGGBB のみ受け付ける。#89b4fa はデフォルトテーマ catppuccin の blue)
rows = [
  ["state_icon", { token = "workspace", fg = "#89b4fa", bold = true }],
  ["branch", "git_status"],
]
