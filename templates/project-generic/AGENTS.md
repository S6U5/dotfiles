<!--
  AGENTS.md の汎用雛形。開発に限らず、AI エージェントに作業してもらう
  あらゆるプロジェクト(文書・調査・データ整理・ナレッジベース等)のルートにコピーして使う。
  開発プロジェクトにはコマンド例入りの templates/project/ 版を使う。
  CLAUDE.md(同じディレクトリの雛形)は @AGENTS.md 参照のみの薄いファイル。

  ◆ 書き方の原則(埋め終わったらこのコメントは削除する)
  - 短く保つ(本体の目安 100〜150 行)。このファイルは毎回エージェントのコンテキストを
    消費し、長いほど指示の遵守率が下がる。詳細が必要なら別ファイルに分けて参照する
  - 中身を見れば分かることは書かない。「見ても分からない決まり」だけを書く
  - 一般論(「丁寧にやって」等)や曖昧語(「適切に」「可能なら」「注意して」)は
    効果がない・確実に無視される。具体的な手順・実例・禁止事項を書く
  - ルールには「なぜ」を添える(例外ケースでエージェントが正しく判断できる)
  - 禁止事項は「〜するな」単体で書かず、必ず代替とセットにする
    (例: 「削除はしない。代わりに archive/ へ移動を提案する」)
  - 古くなった記述は害。メンテしないくらいなら項目ごと削る
  - 機密(キー・内部URL・顧客名)や、公開する場合は業務・個人の文脈を書かない
-->

# プロジェクト名

## 目的

<!-- 1〜3行。エージェントが判断に迷ったときの拠り所。
     例: 「個人の読書メモを蓄積する Obsidian vault。後から検索できることを最優先する」 -->

- 何のためのプロジェクトか
- 誰が・どう使うものか

## よく使う操作

<!-- エージェントに実行してほしい定型作業を、そのまま実行できる形で書く。
     コマンドがあるなら正確に。手順なら番号付きで。無ければこの節は削る
     例(文書): 「新しい記事は templates/article.md をコピーして作成する」
     例(データ): 「集計は scripts/aggregate.py を実行(python3 scripts/aggregate.py)」 -->

- 操作1: `...`
- 操作2: `...`

## 構成

<!-- 主要なフォルダ・ファイルの役割だけ。全部は書かない
     例: 「notes/ — 本文。1トピック1ファイル」「assets/ — 画像。本文からの相対参照」 -->

- `...` — 役割
- `...` — 役割

## ルール

<!-- 中身から読み取れない決まりだけを、理由付きで
     例: 「ファイル名は日付プレフィックス(YYYY-MM-DD-題名.md)。時系列で並ぶようにするため」
     例: 「用語は用語集(glossary.md)に従う。表記ゆれ検索を防ぐため」
     例: 「既存ファイルの章構成は変えない。外部からリンクされているため」 -->

- ルール1(なぜ: ...)
- ルール2(なぜ: ...)

## 完了の条件

<!-- 「何をもって作業完了とするか」を明示し、自己申告の「できました」を防ぐ
     例: 「リンク切れが無いこと(確認方法があればそれも)」
     例: 「新規ファイルにはメタデータ(タグ・日付)が付いていること」 -->

- 条件1
- 条件2

## 境界・禁止事項

<!-- 触ってはいけない場所・やってはいけないこと・既知の罠
     例: 「archive/ は編集禁止(完了済みの記録)」
     例: 「ファイルの削除・改名は提案のみ(実行しない)」 -->

- `...` は編集しない(理由)
- 削除・大規模な構成変更は提案のみとし、実行前に確認を取る
