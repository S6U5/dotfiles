# ==============================================
# OpenAI Codex CLI 設定（最小限）
# ==============================================
# 最小限の設定のみ。他はデフォルト値を使用。

# model = "<モデル名>"  # 未指定ならその時々の既定が使われる
approval_policy = "on-failure"
sandbox_mode = "workspace-write"
